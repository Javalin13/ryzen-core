# E1.25 — Bounded Within-Turn Fallback / Cross-Turn Circuit Gap — 2026-09-17

Status: **PARTIAL GREEN / CROSS-TURN CIRCUIT NOT YET GREEN**

## Evidence

Founder captured PRIME gateway logs for the 18:11–18:12 CEST T3 turn after Cognitive Connection v0.3.1 deployment.

Observed sequence:

1. primary provider `nvidia` / model `poolside/laguna-xs-2.1` failed before delivery with `ResourceExhausted: Worker local total request limit reached (259/32)`;
2. Hermes logged `API call failed (attempt 1/1)` — no same-turn primary retry loop was observed;
3. fallback resolution proceeded toward the configured local Qwen route (`local-ollama` provider naming); the user-visible PRIME response was delivered successfully through fallback;
4. the generic resolver emitted a warning that `local-ollama` has no resolvable API key and would use a `no-key-required` placeholder. This warning is provenance/configuration debt, not the main E1.25 blocker, because the fallback path still produced the turn response.

## Interpretation

### GREEN

- same-turn primary failure is bounded to one attempt;
- fallback continuity works;
- cognition/output remains bounded under degraded primary capacity;
- no retry storm was observed inside the turn.

### NOT YET GREEN

Repeated turns across the session have continued to probe Laguna even after recurring `ResourceExhausted` failures before falling back. Therefore E1.25 is not yet proven as a **health-aware cross-turn circuit**. The remaining question is whether Hermes already has an applicable cooldown/circuit mechanism that does not classify this failure, or whether RYZ3N must add a bounded provider-health guard at the runtime adapter layer.

## Service-level priority

Do not drift into fallback wording or provider-label cosmetics. The next bounded action is to inspect only the existing provider-health/circuit classification path for `ResourceExhausted` and determine the smallest generic seam for temporary unhealthy-primary suppression.

## Portability implication

The desired contract is runtime/model independent:

`provider health signal → bounded unhealthy cooldown → alternate capability route → periodic recovery probe → restore primary when healthy`

This belongs to RYZ3N capacity/runtime adapter policy, not to a specific model name or Telegram behavior.
