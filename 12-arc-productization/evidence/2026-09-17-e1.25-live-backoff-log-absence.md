# E1.25 Live Backoff Log Absence — 2026-09-17

Status: **LIVE CONTRADICTION CONFIRMED — root-cause branch narrowed**

## Live evidence

A journal inspection covering the active PRIME period showed:

- repeated `ResourceExhausted: Worker local total request limit reached` failures on the primary `poolside/laguna-xs-2.1` NVIDIA route;
- fallback completion through `qwen3:0.6b` via the configured local fallback surface;
- **no `Rate-limit backoff level ... cooldown 60 s` log line** in the inspected interval.

## Interpretation

This matters because Hermes source inspection shows that a `FailoverReason.rate_limit` entering `try_activate_fallback()` should arm `_rate_limited_until` and emit a `Rate-limit backoff level ...` log line.

The absence of that live log means at least one of the following is true in the actual PRIME path:

1. the live `ResourceExhausted` exception is not reaching fallback activation as `FailoverReason.rate_limit`;
2. a different error/fallback path bypasses the expected cooldown branch;
3. the event is classified correctly but the health-state arming path is skipped before the successful fallback transition.

This is stronger than a generic cooldown hypothesis and keeps E1.25 focused on the exact runtime path.

## Next bounded proof

Do not alter cooldown constants or create a new circuit yet.

Directly classify the exact observed `ResourceExhausted` failure through Hermes' own `classify_api_error` path, or inspect only the immediate callsite that converts the streaming exception into the `classified.reason` passed to fallback.

## Acceptance target

E1.25 requires:

- same-turn fallback remains bounded;
- a primary exhaustion event creates durable cross-turn health state;
- subsequent turns during the health window stay on the healthy fallback;
- primary recovery is automatic after the health gate expires or reset evidence becomes available.
