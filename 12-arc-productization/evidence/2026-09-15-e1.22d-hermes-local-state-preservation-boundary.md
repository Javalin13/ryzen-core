# E1.22D — Hermes local-state preservation boundary

Date: 2026-09-15
Status: GREEN / preservation gate established
Scope: `/home/prime/hermes-agent`

## Observed state

- Hermes checkout is on detached HEAD `0dfba37b11ff2ca908ae2df85b55f4f4c9b7fd8b`.
- Existing tracked local modification:
  - `plugins/platforms/telegram/telegram_network.py`
- Existing untracked artifacts:
  - `plugins/platforms/telegram/telegram_network.py.bak-20260912-112154`
  - a shell-artifact filename represented by Git status as `="=' "` / `=' ` depending shell rendering.

## Telegram modification classification

The tracked diff adds `os` import and an environment-gated hostname-first ordering path using `HERMES_TELEGRAM_HOSTNAME_FIRST`. This corresponds to the earlier Telegram/network recovery work and is therefore treated as intentional existing runtime state.

## Preservation contract

During E1.22 performance instrumentation:

- do not `git reset`, `git clean`, checkout another ref, or otherwise discard the existing Telegram modification;
- do not delete or overwrite the Telegram backup;
- do not delete the unrelated shell-artifact file as part of this mission;
- do not rewrite repository ownership or trust boundaries merely to make root operate the repository;
- run Git operations as user `prime`;
- touch only the prompt-observability files proven necessary by E1.22;
- create explicit backups / rollback points before modifying those files;
- verify the Telegram diff remains byte-identical after instrumentation.

## Reason

Reliable Excellence requires preserving known-good prior repairs while introducing the smallest reversible intervention. E1.22 must not create collateral network/runtime drift while instrumenting the system-prompt tier boundary.
