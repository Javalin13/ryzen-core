# E1.22K — Cache Controller v0.1.5 activation / isolation GREEN

Date: 2026-09-15
Mission: M3/16 — PRIME performance/cache architecture

## Result

PRIME-only restart completed successfully after v0.1.5 Doctor GREEN.

Observed runtime state:

- `hermes-gateway.service`: active
- PRIME gateway PID: `406273`
- Cargo gateway PID: `71167`
- Cargo process remained unchanged across the PRIME restart.

## Meaning

The provider-independent builder-tier instrumentation introduced for E1.22 is now live in PRIME while Cargo remains isolated. The next acceptance step is one controlled PRIME request followed by inspection of the content-free builder metrics:

- stable chars / share
- context chars / share
- volatile chars / share
- separator chars
- total chars
- full-system-prompt match
- provider latency / cache-read usage

No routing, model, provider, Cargo, Telegram-network, prompt-content, or behavioral changes are part of this checkpoint beyond activation of the shadow instrumentation.
