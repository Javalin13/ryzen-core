# E1.22A — Runtime-Generated Stable Prefix Source Boundary

Date: 2026-09-15
Master: M3/16 — Cargo ARC GREEN
Runtime: PRIME / Hermes

## Result

The 32,837-character stable PRIME prefix measured by the RYZ3N Cache Controller is **not persisted as a `messages` row** in `/home/prime/.hermes/state.db`.

A read-only scan of the `messages` table for 25k–45k text payloads found only large historical `tool` outputs in `content`; no corresponding `system` row was present in `content` or `api_content`.

This corrects the earlier diagnostic that accidentally selected a 143,899-character historical session/task digest from `messages.content` and treated it as a prompt candidate. That object is not the live stable system prefix and must not be compiled or modified.

## Architectural conclusion

The stable PRIME prefix observed at the provider boundary is assembled **at runtime** by Hermes before the provider call. Therefore E1.22 must trace the live runtime prompt-construction path and classify its canonical source blocks rather than mining `state.db` for a stored monolithic prompt.

## Current evidence carried forward

- live stable prefix: 32,837 characters;
- stable share of live message payload: 98.7965%;
- stable-prefix message count: 1;
- provider-reported Laguna input: 20,192 tokens on the latest successful measurement;
- provider-reported cached input: 0;
- latest successful Laguna model-call latency: 4.747837 seconds.

## Next step

Trace the Hermes runtime prompt builder and identify the exact source components that compose the single stable system message. Do not compress, mutate or remove prompt material until source identity and ownership are proven.
