# E1.3A — PRIME state.db schema checkpoint — 2026-09-14

Master: M3/16 — Cargo ARC GREEN
Current: E1 — PRIME/Hermes functional proof
Checkpoint time: 15:09:30 CEST

## Proven live state store

`/home/prime/.hermes/state.db`

The database was modified immediately after the failed Laguna/Hermes verification turn and contains the exact forensic surfaces required to diagnose the unwanted tool call without guessing at Hermes source.

Key tables/fields proven by live schema inspection:

- `sessions`: `model`, `model_config`, `system_prompt`, `system_prompt_hash`, `started_at`, `last_activity_at`, `message_count`, `tool_call_count`, `api_call_count`, source/chat/session metadata.
- `messages`: `session_id`, `role`, `content`, `tool_calls`, `tool_name`, `finish_reason`, `reasoning`, `reasoning_content`, timestamps and active/compaction metadata.
- `system_prompts`: prompt text keyed by hash.
- `session_model_usage`: per-session model/provider usage and API-call/token accounting.
- `gateway_routing`: current routing entry.

Isolation evidence: PRIME uses `/home/prime/.hermes/state.db`; Cargo/NARC/VONDA have separate profile/state databases.

## Diagnostic implication

The next step is no longer broad source grep. Locate the exact session by the literal Founder message `PRIME Laguna verification. Reply exactly: PRIME LAGUNA GREEN`, then inspect only that session's ordered message/tool sequence and prompt-policy lines. This should distinguish among stale mission state, system-prompt/tool-policy bias, model-generated tool call, and recursive-controller behavior.

No runtime mutation performed in E1.3A.
