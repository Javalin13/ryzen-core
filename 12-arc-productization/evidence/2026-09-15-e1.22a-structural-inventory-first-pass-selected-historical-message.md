# E1.22A — Structural Inventory First Pass: Historical Message Selected, Not Stable System Prompt

Date: 2026-09-15
Master: M3/16 — Cargo ARC GREEN
Execution line: E1.22 — verified runtime context compiler

## Result

The first state.db structural-inventory query did **not** select PRIME's ~32.8k stable system/runtime prefix. It selected a much larger historical `messages.content` artifact of 143,899 characters.

The selected content was clearly a historical session/task digest containing sections such as:
- MASTER RUN — TRUTHFUL STATUS REPORT
- Historical Task Snapshot
- Detailed Session Log
- User Messages (verbatim, newest first)
- Cargo ARC Phase 1 Recovery

This is therefore conversation/history material and **must not be used as the source for the E1.22 runtime context compiler**.

## Why this matters

E1.21 telemetry proved the live provider-bound request contains a stable leading block of 32,837 characters, representing ~98.8% of the message payload. E1.22 must compile that real provider-bound stable system/runtime prefix from its canonical source composition, not an unrelated large historical message stored in `state.db`.

## Decision

- Reject the 143,899-character artifact as compiler input.
- Do not compress, summarize, or modify it as part of E1.22.
- Identify the actual persisted/generated system-message source and its composition metadata next.
- Preserve the existing Reliable Excellence rule: compile from canonical source, verify semantic parity, activate only after measured acceptance.

## Anti-drift guard

This failed selector is a diagnostic correction only. The mission remains E1.22 and does not reopen provider selection, networking, Ollama backbone work, or unrelated ARC changes.
