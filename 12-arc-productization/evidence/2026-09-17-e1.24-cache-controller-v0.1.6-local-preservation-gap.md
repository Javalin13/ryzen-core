# E1.24 — Cache Controller v0.1.6 Local Preservation Gap

Date: 2026-09-17
Status: OPEN — preservation required before new telemetry mutation
Master: M3/16 — Cargo ARC GREEN
Current line: E1.24 — effective provider-payload observability

## Evidence

A read-only comparison of `/home/prime/ryzen-core` against `origin/main` showed that the VPS carries legitimate Cache Controller v0.1.6 work not yet represented on the canonical GitHub branch.

The delta is narrowly bounded to:

- version marker `v0.1.3 -> v0.1.6` in `__init__.py`;
- manifest version `0.1.3 -> 0.1.6`;
- addition of `_builder_tier_measurements(...)`;
- inclusion of those builder-tier measurements in `on_pre_api_request(...)` telemetry.

No request mutation, model routing, tool routing, Telegram behavior, or cognition/control behavior was present in this diff.

## Decision

Do **not** overwrite the VPS controller from `origin/main` until this v0.1.6 delta is canonicalized. The next safe action is to transplant only these two tracked files onto a fresh `origin/main` worktree, commit them, and push them to main.

Only after v0.1.6 is preserved canonically should E1.24 add effective post-middleware tool-payload measurements.

## Reliability / portability implication

This closes a documentation and continuity flaw discovered during E1.24: runtime-local engineering state must never remain ahead of canonical source without an explicit preservation checkpoint. Future runtime/model/agent migrations must be able to reconstruct the exact measurement and control state from GitHub without depending on undocumented VPS residue.

## Loading bar

Cognitive Connection remains ~60%. No increase is justified until effective live provider payload narrowing is directly observed.
