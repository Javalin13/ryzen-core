# E1.25 Fallback Reason Callsite Proof — 2026-09-17

Status: **ROOT-CAUSE NARROWING**

The live error-handling block in `agent/conversation_loop.py` explicitly computes `classified.reason`, identifies `FailoverReason.rate_limit` as eagerly fall-backable, and calls:

`agent._try_activate_fallback(reason=classified.reason)`

at the main eager-fallback callsite.

This proves that the principal live error-handling path does preserve the classified reason into fallback activation. Therefore the previously suspected generic loss of `classified.reason` at that callsite is not supported.

However, multiple other fallback callsites still invoke `_try_activate_fallback()` with no explicit reason. The live PRIME logs show `ResourceExhausted` without the expected `Rate-limit backoff level ...` event, so the remaining high-probability explanation is that the real NVIDIA/Laguna failure is reaching a later or alternate fallback callsite without the classified reason, or the eager branch is bypassed (for example because the credential-pool recovery gate blocks it) and fallback occurs later through a reasonless path.

Next bounded proof: inspect only the lower fallback blocks around the reasonless callsites after the eager branch (not broad source archaeology), especially the blocks around `conversation_loop.py` ~6158 and ~6372, to identify which one can handle the observed `ResourceExhausted` after the eager branch is bypassed.

Acceptance target remains: primary `ResourceExhausted` must arm cross-turn health state before successful fallback so subsequent turns do not immediately re-probe the same exhausted primary.
