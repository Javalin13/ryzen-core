# E1.19A — PRIME repo sync GREEN

Date: 2026-09-15
Checkpoint: ~14:02 CEST
Mission: M3/16 — Cargo ARC GREEN
Substep: E1.19A — stage RYZ3N Cache Controller shadow mode

## Result

The earlier staging command was blocked because root did not own `/home/prime/ryzen-core` and root also lacked the GitHub SSH identity. The correct fix was to operate the repository as its actual owner/runtime user `prime`.

Command:

```bash
sudo -u prime -H git -C /home/prime/ryzen-core pull --ff-only
```

Result:

- remote authentication succeeded using PRIME's existing GitHub SSH identity;
- repository fast-forwarded from `a349fb1` to `5d0cb11`;
- E1.17/E1.18 evidence, Hermes telemetry adapter direction, Reliable Excellence updates, and the RYZ3N cache-controller source arrived on the VPS;
- no SSH key was added to root;
- no remote was changed;
- no runtime/config/model change occurred.

## Architecture significance

The deployment path remains aligned with the canonical operating model:

GitHub source truth -> PRIME-owned working copy -> controlled staging -> runtime validation.

The previous root ownership/auth issue was an operator-context blocker only, not a cache-controller or Hermes defect.

## Next

Continue E1.19A by staging the plugin into `/home/prime/.hermes/plugins/ryz3n-cache-controller` and run `hermes plugins doctor ... --ci` before enablement.
