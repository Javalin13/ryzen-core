# E1.22I — Provider-independent builder-tier instrumentation GREEN

Date: 2026-09-15
Mission: M3/16 — CARGO ARC GREEN
Track: E1.22 performance/cache system

## Result

E1.22I is GREEN.

A provider-independent, content-free measurement seam was added at the authoritative Hermes system-prompt builder rather than at provider/cache-specific layers.

### Builder instrumentation
`agent/system_prompt.py::build_system_prompt()` now records only numeric tier metrics on the agent:
- `stable_chars`
- `context_chars`
- `volatile_chars`
- `total_chars`
- `separator_chars`

The actual prompt text is unchanged.

### Hook exposure
`agent/conversation_loop.py` exposes only the numeric `system_prompt_builder_tier_metrics` object to the existing `pre_api_request` observer hook.

### RYZ3N Cache Controller
Local source advanced to v0.1.5 and consumes the builder metrics to emit:
- `builder_tier_metrics_available`
- `builder_stable_char_count`
- `builder_context_char_count`
- `builder_volatile_char_count`
- `builder_separator_char_count`
- `builder_total_char_count`
- `builder_total_matches_system_prompt`
- `builder_stable_share`
- `builder_context_share`
- `builder_volatile_share`

No prompt text is persisted by this instrumentation.

## Verification

- Patch applied: `BUILDER_TIER_PATCH_APPLIED`
- Python compile: `PY_COMPILE_GREEN`
- Scope of Hermes changes limited to `agent/system_prompt.py` and `agent/conversation_loop.py`
- Scope of RYZ3N changes limited to Cache Controller implementation/manifest
- Existing Telegram repair preserved byte-for-byte

Telegram baseline SHA-256 remains:
`e307f1fc4c57f11207ca3860d9c107ca32be4249691aeb49d47af30aa8ca8478`

## Architectural significance

This measurement path is provider-independent because it runs at `build_system_prompt()` before provider cache-capability decisions. It therefore applies consistently across Laguna, Qwen, Super, Ultra, Anthropic, or future model/provider lanes.

This is the correct basis for the next intervention: quantify the canonical `stable/context/volatile` weights live, then target real context reduction while preserving identity/governance and full canonical source truth.
