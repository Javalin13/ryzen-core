# E1.22D — Content-Free Authoritative Static-Tier Instrumentation GREEN

Date: 2026-09-15
Mission: M3/16 Cargo ARC GREEN — performance/reliability substrate
Status: instrumentation-preactivation GREEN

## Purpose

Expose enough of Hermes' authoritative `_cached_system_prompt_static` boundary to measure the real static-vs-dynamic system-prompt share without exposing or persisting raw prompt content.

## Proven implementation

Hermes local runtime (`/home/prime/hermes-agent`, detached HEAD `0dfba37b11ff2ca908ae2df85b55f4f4c9b7fd8b`) was patched only in `agent/conversation_loop.py` so the existing `pre_api_request` observer hook receives:

- `system_prompt_static_char_count`
- `system_prompt_static_matches_system_prompt`

The raw `_cached_system_prompt_static` string is **not** passed across the hook boundary.

RYZ3N Cache Controller local source was advanced to v0.1.4 to derive only aggregate telemetry:

- `hermes_system_prompt_char_count`
- `hermes_static_system_prefix_available`
- `hermes_static_system_prefix_char_count`
- `hermes_static_prefix_matches_system_prompt`
- `hermes_dynamic_system_tail_char_count`
- `hermes_static_share_of_system_prompt`

No raw static-prefix text or fingerprint of raw static content is required by this v0.1.4 measurement path.

## Verification

Observed Founder/VPS output:

- `CONTENT_FREE_PATCH_APPLIED`
- `PY_COMPILE_GREEN`
- Hermes diff limited to the two content-free observer fields above
- Cache Controller diff limited to v0.1.4 authoritative-tier aggregate measurements
- Telegram repair hash preserved byte-for-byte:
  `e307f1fc4c57f11207ca3860d9c107ca32be4249691aeb49d47af30aa8ca8478`

## Preservation boundary

Pre-existing Hermes local state remains untouched:

- detached HEAD intentionally preserved
- `plugins/platforms/telegram/telegram_network.py` repair preserved
- existing Telegram backup preserved
- unrelated untracked shell artifact preserved
- no reset/clean/checkout/rebuild-from-scratch performed

## Acceptance meaning

This checkpoint proves the instrumentation is syntactically valid, behavior-neutral by construction, privacy-hardened, and ready for Plugin Doctor + PRIME-only activation. It does **not** yet prove the measured static-tier size; that requires one live request after activation.

## Next gate

1. deploy local Cache Controller v0.1.4 to PRIME plugin directory;
2. Plugin Doctor must remain GREEN (`0 tools / 3 hooks`);
3. PRIME-only restart;
4. one controlled live request;
5. read authoritative static-tier metrics;
6. only then design the first optimization intervention.
