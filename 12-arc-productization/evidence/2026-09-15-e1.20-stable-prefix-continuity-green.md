# E1.20 — Stable Prefix Continuity GREEN

Date: 2026-09-15
Master: M3/16 — Cargo ARC GREEN
Runtime under test: PRIME / Hermes
Cache Controller: ryz3n-cache-controller v0.1.1 shadow mode

## Result

E1.20 is GREEN.

Two changing PRIME turns produced different full request fingerprints and different history-prefix fingerprints while preserving the same stable-prefix fingerprint across both Laguna/NVIDIA attempts.

Stable prefix SHA-256:

`12fb6d0a9e76898023bad62b897117309d081332f210bee76ea039e4a76b4496`

Observed continuity:

- Turn A Laguna pre-request: stable-prefix match = true
- Turn B Laguna pre-request: stable-prefix match = true
- Full request fingerprints changed between turns
- History-prefix fingerprints changed between turns
- Stable-prefix fingerprint remained identical

## Architectural significance

This experimentally proves that PRIME has a reusable stable constitutional/runtime prefix that remains invariant while conversation history and the newest user request change.

That is the required foundation for provider-independent cache-aware execution, context compilation and future cache-efficiency measurement.

## Separate runtime issue observed

Turn B had not completed at the moment of fingerprint proof. This does not invalidate E1.20 because the `pre_api_request` event is sufficient to prove stable-prefix continuity. Response latency/provider completion remains a separate routing/capacity concern.

Earlier Turn A forensic evidence showed:
- Laguna/NVIDIA failed after ~2.316 s;
- local Qwen fallback then consumed ~89.665 s;
- the Cache Controller was not responsible for that latency.

## Decision

Proceed from prefix identification to measurable cache-efficiency work. Do not conflate provider availability/fallback latency with prefix-continuity acceptance.
