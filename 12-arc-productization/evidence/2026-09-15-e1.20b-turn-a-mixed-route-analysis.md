# E1.20B — Turn A mixed-route analysis

**Date:** 2026-09-15
**Master:** M3/16 — Cargo ARC GREEN
**Substep:** E1.20B — fingerprint continuity / cache observability

## Live result

PRIME received `Reply exactly: PRIME CACHE A` after v0.1.1 deployment.

The turn crossed two provider lanes:

1. `nvidia` / `poolside/laguna-xs-2.1`
   - pre-api fingerprinting succeeded with non-empty hashes;
   - request fingerprint: `88af7d9008b23f769854c0a51c6c75c9c5c0b51dd1966c985dbccb31d391ccf7`;
   - history-prefix fingerprint: `680082fe4fb42fbb2b948e0e0a2a68cbf5bb4182564e2311890ad49d0eb0f297`;
   - stable-prefix fingerprint: `12fb6d0a9e76898023bad62b897117309d081332f210bee76ea039e4a76b4496`;
   - provider failure after ~2.316 s.

2. `local-ollama` / `qwen3:0.6b`
   - fallback request fingerprinting also succeeded;
   - provider call completed in ~89.665 s;
   - input tokens: 2050;
   - cached input tokens: 0;
   - output tokens: 157.

## Conclusion

The large latency increase was not introduced by the Cache Controller. Laguna failed quickly; the dominant delay came from the local Qwen emergency fallback (~89.7 s).

The v0.1.1 fingerprint correction is confirmed: request/history/stable-prefix hashes are now non-empty and provider-lane specific. The next acceptance test should compare consecutive Laguna pre-api events for stable-prefix continuity, regardless of whether a later fallback occurs, because the fingerprint is captured before provider execution.

## Architecture implication

Telemetry is already exposing the future capacity-router decision data we need: per-lane latency, provider failure, fallback behavior, token usage, and stable-prefix identity. This supports the planned health-aware free-endpoint routing and Founder capacity dashboard without storing raw prompt/response content.
